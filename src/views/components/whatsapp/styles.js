import styled from "styled-components";

export const Wrapper = styled.div`
	.chat-container {
		border-radius: 5px;
		padding: 15px;
		position: fixed;
		bottom: 10px;
		right: 10px;
		background-color: #fff5fb;
		box-shadow: 6px 6px #9e005d;
		display: flex;
		align-items: center;
		cursor: pointer;
		gap: 10px;
	}

	.bold-font {
		font-weight: bold;
	}
`;

export const Content = styled.div``;
